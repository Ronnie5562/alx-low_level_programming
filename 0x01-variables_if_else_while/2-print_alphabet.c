#include <stdio.h>

/**
 * main - Entry point
 * Description: lowercase alphabet
 * Return: Always 0 (success)
 */
int main(void)
{
	for (char x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
